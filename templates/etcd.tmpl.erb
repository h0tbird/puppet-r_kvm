- targets:{{range gets "/hosts/core/master/*"}}
  - {{base .Key}}:2379{{end}}
  labels:
    group: masters
- targets:{{range gets "/hosts/core/slave/*"}}
  - {{base .Key}}:2379{{end}}
  labels:
    group: proxies
