#------------------------------------------------------------------------------
# This file is meant to be sourced by bash scripts:
#------------------------------------------------------------------------------

readonly ID='<%= @config["cell-#{@cell}"]['boot']['ID'] %>'
readonly IMAGE='<%= @config["cell-#{@cell}"]['boot']['IMAGE'] %>'
readonly HOSTNAME='<%= @config["cell-#{@cell}"]['boot']['HOSTNAME'] %>'
readonly DATA_DIR='<%= @config["cell-#{@cell}"]['boot']['DATA_DIR'] %>'
readonly DHCP_RANGE='<%= @config["cell-#{@cell}"]['boot']['DHCP_RANGE'] %>'
readonly DHCP_OPTION='<%= @config["cell-#{@cell}"]['boot']['DHCP_OPTION'] %>'
readonly DNS_UPSTREAMS='<%= @config["cell-#{@cell}"]['boot']['DNS_UPSTREAMS'] %>'
readonly DOMAIN='<%= @config["cell-#{@cell}"]['boot']['DOMAIN'] %>'
readonly IP='<%= @config["cell-#{@cell}"]['boot']['IP'] %>'
readonly IP_PREFIX='<%= @config["cell-#{@cell}"]['boot']['IP_PREFIX'] %>'
readonly GATEWAY='<%= @config["cell-#{@cell}"]['boot']['GATEWAY'] %>'
readonly HOST_BRIDGE='<%= @config["cell-#{@cell}"]['boot']['HOST_BRIDGE'] %>'
