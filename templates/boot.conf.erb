#------------------------------------------------------------------------------
# This file is meant to be sourced by bash scripts:
#------------------------------------------------------------------------------

readonly ID='<%= @config['boot']['ID'] %>'
readonly IMAGE='<%= @config['boot']['IMAGE'] %>'
readonly HOSTNAME='<%= @config['boot']['HOSTNAME'] %>'
readonly DATA_DIR='<%= @config['boot']['DATA_DIR'] %>'
readonly DHCP_RANGE='<%= @config['boot']['DHCP_RANGE'] %>'
readonly DHCP_OPTION='<%= @config['boot']['DHCP_OPTION'] %>'
readonly DNS_UPSTREAMS='<%= @config['boot']['DNS_UPSTREAMS'] %>'
readonly DOMAIN='<%= @config['boot']['DOMAIN'] %>'
readonly IP='<%= @config['boot']['IP'] %>'
readonly IP_PREFIX='<%= @config['boot']['IP_PREFIX'] %>'
readonly GATEWAY='<%= @config['boot']['GATEWAY'] %>'
readonly HOST_BRIDGE='<%= @config['boot']['HOST_BRIDGE'] %>'
