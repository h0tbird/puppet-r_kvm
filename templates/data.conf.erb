#------------------------------------------------------------------------------
# This file is meant to be sourced by bash scripts:
#------------------------------------------------------------------------------

readonly ID='<%= @config["cell-#{@cell}"]['data']['ID'] %>'
readonly IMAGE='<%= @config["cell-#{@cell}"]['data']['IMAGE'] %>'
readonly HOSTNAME='<%= @config["cell-#{@cell}"]['data']['HOSTNAME'] %>'
readonly DATA_DIR='<%= @config["cell-#{@cell}"]['data']['DATA_DIR'] %>'
readonly COREOS_CHANNEL='<%= @config["cell-#{@cell}"]['data']['COREOS_CHANNEL'] %>'
readonly HOST_BRIDGE='<%= @config["cell-#{@cell}"]['data']['HOST_BRIDGE'] %>'
